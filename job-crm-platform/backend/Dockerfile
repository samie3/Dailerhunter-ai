FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY . .
RUN npx prisma generate --schema=../database/prisma/schema.prisma 2>/dev/null || true
EXPOSE 4000
CMD ["node", "src/index.js"]
